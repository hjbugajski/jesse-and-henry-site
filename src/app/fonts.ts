import { Alice, Lato } from 'next/font/google';

export const alice = Alice({ weight: '400', subsets: ['latin'], variable: '--font-heading' });
export const lato = Lato({ weight: ['300', '400', '700'], subsets: ['latin'], variable: '--font-body' });
